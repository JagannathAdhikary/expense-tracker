node_modules/
dist/
dev-dist/
.vite/
.DS_Store

# Local Supabase credentials — never commit. Use .env.example as the template.
.env
.env.local

# Supabase CLI local link state (project ref, versions) — machine-local, not for git.
supabase/.temp/

# IDE
.idea/
.vscode/
